# Frontend — Flutter

> **Status**: Flutter project will be scaffolded before Phase 4.

Flutter is not yet installed on this machine. The full Flutter project (Android + Web targets) with the ServiceWala chat UI will be created when we reach Phase 4 of the build.

## Planned Structure
- `lib/main.dart` — App entry point, chat screen
- `lib/config.dart` — Base URL configuration (web / emulator / real phone)
- `lib/api_service.dart` — HTTP client for the backend
- `pubspec.yaml` — Dependencies

## Prerequisites
- Flutter SDK (stable channel)
- Android SDK (for Android builds)
- Chrome (for web preview during development)
